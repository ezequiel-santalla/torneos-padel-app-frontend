const BASE = 'http://localhost:8080/api/v1';

export const AUTH_URLS = {
  LOGIN: `${BASE}/auth/login`,
  REGISTER: `${BASE}/auth/register`,
  CHECK_STATUS: `${BASE}/auth/check-status`,
};

export const ENUM_URLS = {
  ENUMS: `${BASE}/enums`,
};

export const TOURNAMENT_URLS = {
  TOURNAMENTS: `${BASE}/tournaments`,
};

export const PLAYER_URLS = {
  PLAYERS: `${BASE}/players`,
};

export const PLAYER_STANDING_URLS = {
  STANDINGS: `${BASE}/player-standings`,
};

export const PLAYER_RANKING_URLS = {
  RANKINGS: `${BASE}/player-rankings`,
};



